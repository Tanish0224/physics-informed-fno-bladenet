# Limitations

This surrogate model is not a strict direct CFD substitute and does not guarantee global mass conservation. The selected physics weighting parameter ($\lambda=1.0$) was optimal for this validation split, but is not unconditionally universal.
